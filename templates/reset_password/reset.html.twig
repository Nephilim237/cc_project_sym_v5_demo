{% extends 'base.html.twig' %}

{% block title %}Nouveau mot de passe{% endblock %}

{% block body %}
    <section>
        <div class="container py-5">
            <div class="row">
                <div class="col-md-6 col-sm"></div>
                <div class="col-md-6 col-sm ms-auto">
                    <div class="card">
                        <div class="card-header">
                            <h1 class="b-title fs-4 mb-2">Réinitialisez votre mot de passe</h1>
                        </div>
                        <div class="card-body border-top border-warning">

                            {{ form_start(resetForm) }}
                            <div class="mb-3">
                                {{ form_row(resetForm.plainPassword) }}
                            </div>
                            <div class="d-flex justify-content-end">
                                <button class="cc-btn cc-btn-outline">Changer<i class="fas fa-sync"></i></button>
                            </div>
                            {{ form_end(resetForm) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
